# shimajiro-lp

巧虎手偶（パペット）のランディングページプロジェクト。

## 概要

- 巧虎手偶（しまじろうパペット）の販売用ランディングページ
- 単一ファイル構成（`index.html` にHTML/CSS/JSを内包）
- ターゲット言語：繁体中文（`lang="zh-TW"`）

## 技術スタック

- **HTML / CSS / Vanilla JS**（フレームワーク不使用）
- **GSAP 3.12.5** + **ScrollTrigger**（CDN読み込み）
  - Heroの飛び出しアニメーション
  - スクロール連動のフェードイン
  - CTA価格のカウントアップ
- **Google Fonts**: Noto Sans TC（400/700/900）

## デザイン仕様

- スマホ最適化：`max-width: 430px` で中央寄せ
- 下部固定CTAバー（常時表示）
- カラー：オレンジ系（#FF6B35〜#FF9D2E）+ 黄色背景（#FFE08A）

## 構成セクション

1. **Hero** — 巧虎手偶が下から飛び上がる演出 + 価格 + 購入ボタン
2. **Effects** — 3つの効果（① 安撫 / ② 鼓勵 / ③ 建立生活習慣）
3. **Features** — 3つの特徴（外型超吸睛 / 觸感柔軟佳 / 輕鬆可清洗）
4. **CTA** — 価格カウントアップ + 購入ボタン

## リンク・アセット

- 購入リンク：`https://store.benesse.com.tw/products/巧虎手偶`
- 画像：すべて `images/` フォルダ内（背景透明PNGを多用）
  - 背景：`パペットLP_背景.png`
  - ボタン：`パペットLP_ボタン.png`
  - キャラクター：`パペット_背景透明_*.png` シリーズ

## 価格表記

- 原価：NT$399
- 特価：NT$299

## 作業時の方針

- ユーザーへの回答は**日本語**で行う
- 作業は**途中確認なしで一気に**進める
- 画像アセット内のオブジェクト位置を CSS で扱う場合（例: 擬似要素の `inset`、
  クリッピング座標、装飾を避けた発光・縁取り）、目視推測せず Pillow で α/RGB を
  解析して実測値を使う。`pip install Pillow` 済みのワンライナーで bbox を出せる。
